module.exports = (array, chunkSize) => {
  let chunks = [];
  //Slice every "chunkSize" items and join them into a string
  for (let i = 0; i < array.length; i += chunkSize) {
    chunks.push(array.slice(i, i + chunkSize).join() + (i + chunkSize < array.length ? "," : ""));
  }
  return chunks;
};
