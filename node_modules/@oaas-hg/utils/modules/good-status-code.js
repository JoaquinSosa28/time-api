const minCode = 200;
const maxCode = 299;

/**
 * Evaluate whether a HTTP status code is good or not
 * @param {Number} code - Code to evaluate
 */
module.exports = (code) => {
  return code >= minCode && code <= maxCode;
};
